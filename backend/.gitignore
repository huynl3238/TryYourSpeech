node_modules/
.env
uploads/audio/*
uploads/tmp/*
!uploads/audio/.gitkeep
!uploads/tmp/.gitkeep

# Bộ đo độ chính xác chấm AI. Chỉ dùng để thử trên máy cá nhân, không phải một
# phần của ứng dụng — app chạy được đầy đủ mà không cần ba thư mục này.
# eval-data riêng đã 1.7 GB audio, đưa vào git là hỏng repo.
eval-data/
how_to_get_audio/
scripts/

# Tập con của eval-data, dựng tạm khi cần đo trên vài bài. Cũng là audio nặng.
eval-subset/
